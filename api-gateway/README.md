API Gateway module

This module provides a Spring Cloud Gateway front door for the system.

Dependencies included: Spring Cloud Gateway, Eureka Discovery Client, Spring Boot Actuator, OAuth2 Resource Server.

Adjust `application.yml` (Eureka URL, `jwk-set-uri`) before running.
